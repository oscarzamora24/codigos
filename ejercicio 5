def bin(decimal):
    bin = ''
    while decimal // 2 != 0:
        bin = str(decimal % 2) + bin
        decimal = decimal // 2
    return str(decimal) + bin
numero = int(input('ingrese un valor para convertirlo a binario: '))
print(bin(numero))
